// removeChild(element)     it is like appendchild but use to remove
// remove(element)          it is like append but use to remove

let body = document.querySelector('body');
body.removeChild(btn);

