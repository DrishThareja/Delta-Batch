// Properties ==>
//      -> Math.PI          = Gives pi value
//      -> Math.E           = Gives E value

// Methods ==>
//      -> Math.abs(n)      = Gives every number in +ve
//      -> Math.Pow(a, b)   = (2,4) means 2 ki power 4
//      -> Math.floor(n)    = roundoff to the nearest smallest integer
//      -> Math.ceil(n)     = roundoff to the nearest largest integer
//      -> Math.random()    = Gives random value between 0 to 1 , {excluding 1}

