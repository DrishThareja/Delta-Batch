// sort : sort an array

let days = ["monday", "wednesday", "tuesday", "friday", "thrusday", "sunday", "saturday"];
sort = days.sort();
console.log(sort);

let squares = [25, 16, 4, 49, 36, 9];
sq = squares.sort();
console.log(sq);        // sort doesn't work on numbers

// sort method converts elements to string so it did n't work on numbers
